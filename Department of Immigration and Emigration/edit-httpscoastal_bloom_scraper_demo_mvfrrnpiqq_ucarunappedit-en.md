# Edit Page Content



Edit the HTML below — headings (<h1>/<h2>),
      paragraphs (<p>), lists (<ul>/<li>),
      tables (<table>), and quotes (<blockquote>) all work.
      This is exactly the content the scraper picks up — it does not include the site's
      nav or footer.

manually added 1 - CMS 